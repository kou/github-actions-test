name: Windows
on:
  - push
jobs:
  test:
    name: Test
    runs-on: windows-latest
    steps:
      - name: Set culture
        run: |
          echo $Env:PSCulture
          Get-Culture
          [cultureinfo]::CurrentCulture = "ja-JP"
          Get-Culture
